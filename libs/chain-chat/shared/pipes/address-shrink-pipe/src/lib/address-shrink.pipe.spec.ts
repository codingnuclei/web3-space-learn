import { AddressShrinkPipe } from './address-shrink.pipe';

describe('AddressShrinkPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressShrinkPipe();
    expect(pipe).toBeTruthy();
  });
});
